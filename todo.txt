- Do a platform check
- detect fi package is signed (do not sign again!) (use python-debian)
- detect if  dpkg-sig is there  
- detect if apt-ftparchive is there
- detect if gpg is there
- buildin webserver
- tree.walk


dependencies:
apt-utils
dpkg-sig